grammar;


pub Region: (String, Vec<String>) = {
    "region" <name:Ident> "{" <funcs:Function*> "}" => (name, funcs)
};



Function: String = {
    "function" <name:Ident> "(" ")" => name
};


Ident: String = {
    r"[a-zA-Z_][a-zA-Z0-9_]*" => <>.to_string()
};
